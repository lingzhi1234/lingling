# define _CRT_SECURE_NO_WARNINGS 1
# include<stdio.h>
int main()
{
	int i,n,a=1;
	printf("请输入一个整数n\n");
	scanf("%d", &n);
	if (n == 0 )
	{
		printf("%d的阶层为%d", n, a);
	}
	for (i = 1; i <=n; i++)
	{
		a= a * i ;
		printf("%d的阶层为%d", n, a);
	}
	if (n < 0)
	{
		printf("负数不存在阶层");
	}
	return 0;
}
